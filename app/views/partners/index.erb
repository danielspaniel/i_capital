<h3 class="text-2xl font-bold">Partners</h3>
<div class="mt-6 p-4">
  <% @partners.each do |partner| %>
    <%= link_to partner.name, partner_investors_path(partner), class: "block bg-blue-600 text-white font-semibold py-2 px-4 w-40 rounded hover:bg-blue-700 transition" %>
  <% end %>
</div>